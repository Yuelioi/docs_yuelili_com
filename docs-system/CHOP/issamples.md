## 描述

Returns 1 if the Vex CHOP‘sUnit Menu is currently set to 'samples',  
0 otherwise.

Context(s) | [chop](../contexts/chop.html)  
---|---  
  

```c
int  issamples()
```


Returns 1 if the Vex CHOP‘sUnit Menu is currently set to 'samples',
0otherwise.

如果Vex CHOPâs Unit Menu当前设置为 "样本"，则返回1，否则返回0。

