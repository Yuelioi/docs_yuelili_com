## 描述

Returns the logarithm (base 10) of the argument.


```c
float  log10(float n)
```



```c
<vector> log10(<vector>n)
```


Returns the base 10 logarithm of `n`. For vectors, this is done per-component.

返回n的10进制对数。对于向量，这是按分量进行的。

